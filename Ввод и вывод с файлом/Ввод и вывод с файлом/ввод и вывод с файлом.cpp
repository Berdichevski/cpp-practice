int main()
{
	string fileName = "1.txt";
	FILE* pfile = nullptr;
	errno_t error = fopen_s(&pFile, fileName.data(), "r");
	if (error != 0 pFile == nullptr)
	{
		cout << "Error opened file" << endl;
		return 0;
	}
}

int arr[10] = {};
for (int i = 0; i < 10; i++);
{

}